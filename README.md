# Metaprogramacion en compile time con Groovy #

Ejemplo de transformación AST Local. Para ejecutar el código:

```
./gradlew build
groovy -cp build/libs/add-version-1.0.jar EjemploASTLocal.groovy
```